package PP.Benchmark;

public class TwelveCases {

    //sort already sorted list. in ascending order!.
    //sort a list which is already sorted in descending order.
    //sort gradually exponentially growing big data and compare it with the multiple sorting algorithms.(done).
}
